extends layout

block content
    .map-title Citizen Engagement Map
    .flx-map(ng-controller="CitizenMapController")
        leaflet(class="map" id="map" markers="mapMarkers" legend="legend" defaults="defaults" center="mapCenter")
