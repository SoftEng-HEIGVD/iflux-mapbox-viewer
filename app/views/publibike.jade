extends layout

block content
    .map-title Publibike Map
    .flx-map(ng-controller="PublibikeMapController")
        leaflet(class="map" id="map" markers="mapMarkers" legend="legend" defaults="defaults" center="mapCenter")
